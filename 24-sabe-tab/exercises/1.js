// Create a function, getFirst(arr), that returns the fisrt item in the array

const array = ['hi', 'mi', 'name', 'is'];

function getFirst(arr) {
  let first = arr[0];
  return first;
}

console.log(getFirst(array));



// Call it with an array as an argument to verify that it works